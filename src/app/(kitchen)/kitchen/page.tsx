import { SurfacePlaceholder } from "@/components/surface-placeholder";

export default function KitchenPage() {
  return (
    <SurfacePlaceholder
      surface="Kitchen"
      description="Kitchen display system (KDS) — realtime tickets, bump/recall, station routing. Built in Phase 3."
    />
  );
}
