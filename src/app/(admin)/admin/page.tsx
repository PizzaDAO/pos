import { SurfacePlaceholder } from "@/components/surface-placeholder";

export default function AdminPage() {
  return (
    <SurfacePlaceholder
      surface="Admin"
      description="Tenant back office — menu management, inventory, reports, staff/shifts, end-of-day. Built in Phase 5."
    />
  );
}
