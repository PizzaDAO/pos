import { SurfacePlaceholder } from "@/components/surface-placeholder";

export default function PlatformPage() {
  return (
    <SurfacePlaceholder
      surface="Platform"
      description="Super-admin (us) — tenant list, health, subscription billing, support impersonation (audited). Outside tenant RLS scope. Built in Phase 6."
    />
  );
}
